// 一些样式公共代码封装
no-wrap()
    text-overflow ellipsis
    overflow hidden
    white-space nowrap

flex($jc)
    display flex
    justify-content $jc
    align-items center
bg-img($url)
    background-image:url($url)